meta {
  name: Add user
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/admin/users/new
  body: json
  auth: inherit
}

body:json {
  {
    "name": "timmy",
    "email": "a@a.com"
  }
}

settings {
  encodeUrl: true
}
