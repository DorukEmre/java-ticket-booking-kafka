CREATE TABLE `order` (
	id VARCHAR(8) PRIMARY KEY,
	total DECIMAL(10,2),
	placed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	customer_id BIGINT,
  status VARCHAR(20) NOT NULL DEFAULT 'VALIDATING',
  CHECK (status != 'COMPLETED' OR total IS NOT NULL)
);