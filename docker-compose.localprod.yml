
services:
  ticket-caddy:
    ports:
      - "5443:5443"
      - "8443:8443"
    volumes:
      - ./Caddyfile.localprod:/etc/caddy/Caddyfile:ro
  
  catalog-service:
    environment:
      API_SERVER_URL: ${API_SERVER_URL_LOCAL}

  cart-service:
    environment:
      API_SERVER_URL: ${API_SERVER_URL_LOCAL}

  order-service:
    environment:
      API_SERVER_URL: ${API_SERVER_URL_LOCAL}
